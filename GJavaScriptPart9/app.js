/* let smallImages = document.getElementsByClassName("oldImg");

for(let i = 0; i < smallImages.length; i++){
    smallImages[i].src = "assets/spiderman_img.png";
    console.log(`value of image Node. ${i} is Changed.`);
} */


                          /* Query Selector*/ 
        // console.dir(document.querySelector("h1"));

        // console.dir(document.querySelector("#description"));

        // console.dir(document.querySelector(".oldImg "))

        // console.dir(document.querySelectorAll("div a"));



                               // Manipulating Attribute 

       //1:- getAttribute(attributeName)
           // Kisi element ka attribute padhne ke liye.

            /*syntax :- let link = document.getElementById("myLink");
                      let hrefValue = link.getAttribute("href");
                      console.log(hrefValue);*/


        //2:-  setAttribute(attributeName, value)
            // Naya attribute set karne ya existing ko update karne ke liye.

            /* Syntax :- 
                    link.setAttribute("href", "https://www.flipkart.com"); */


                         // Navigation in JavaScript
                        /*  DOM Navigation Summary */

/* | Property                 | Kya karta hai?                          |

   | `parentElement`          | Ek element ke direct parent ko deta hai |

   | `children`               | Parent ke saare child elements deta hai |

   | `firstElementChild`      | First child element deta hai            |

   | `lastElementChild`       | Last child element deta hai             |

   | `nextElementSibling`     | Next bhai element deta hai (same level) |

   | `previousElementSibling` | Pehle wala bhai element deta hai        | */





                                    // Practice Qs
                                    







